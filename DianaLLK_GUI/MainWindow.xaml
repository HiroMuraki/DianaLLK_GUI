<Window
    x:Class="DianaLLK_GUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:View="clr-namespace:DianaLLK_GUI.View"
    xmlns:ViewModel="clr-namespace:DianaLLK_GUI.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DianaLLK_GUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="1280"
    Height="720"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    Loaded="Window_Loaded"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            ResizeBorderThickness="5"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="ExpandTokenStackButton" TargetType="Button">
            <Setter Property="FontSize" Value="30" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextTheme}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10,0,0,10">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.65" />
                                <Setter Property="Foreground" Value="Transparent" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextTheme}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.65" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid
        x:Name="GridRoot"
        AllowDrop="True"
        DragDrop.DragEnter="Window_DragEnter"
        DragDrop.DragLeave="Window_DragLeave"
        DragDrop.Drop="Window_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="1" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid
            Grid.Row="0"
            Background="{Binding GameTheme, Converter={StaticResource GameThemeToColor}}"
            MouseDown="Window_Move">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition />
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="0"
                Click="ExpandGameSetter_Click"
                Content="="
                Style="{StaticResource TitleButtonStyle}"
                ToolTip="设置" />
            <Button
                Grid.Column="1"
                Click="GameSave_Click"
                Content="{Binding Game.SkillPoint}"
                Style="{StaticResource TitleButtonStyle}"
                ToolTip="当前技能点&#13;点击保存以游戏" />
            <View:SkillGroupControl
                Grid.Column="2"
                HorizontalAlignment="Center"
                SkillActived="ActiveSkill_Click" />
            <Button
                Grid.Column="3"
                Click="Window_Minimum"
                Content="&#x2501;"
                Style="{StaticResource TitleButtonStyle}" />
            <Button
                Grid.Column="4"
                Click="Window_Maximum"
                Content="&#x2610;"
                Style="{StaticResource TitleButtonStyle}" />
            <Button
                Grid.Column="5"
                Click="Window_Close"
                Content="&#x2573;"
                Style="{StaticResource TitleButtonStyle}" />
        </Grid>
        <Border Grid.Row="1" Background="{StaticResource TextTheme}" />
        <Grid Grid.Row="2">
            <Border>
                <Border.Background>
                    <ImageBrush ImageSource="{Binding GameTheme, Converter={StaticResource GameThemeToBackground}}" Stretch="UniformToFill" />
                </Border.Background>
            </Border>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <View:GameStatisticsControl
                    x:Name="GameStatistics"
                    Grid.Column="0"
                    Width="0"
                    Confirmed="GameStatistics_Confirmed" />
                <Grid Grid.Column="1">
                    <Viewbox Grid.Row="1" Grid.Column="1">
                        <Grid Margin="30">
                            <Border
                                Background="{Binding GameTheme, Converter={StaticResource GameThemeToColor}}"
                                BorderBrush="{StaticResource TextTheme}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Opacity="0.65" />
                            <Canvas x:Name="TokensLayout" />
                        </Grid>
                    </Viewbox>
                    <Button
                        Width="30"
                        Height="200"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="{Binding GameTheme, Converter={StaticResource GameThemeToColor}}"
                        Click="ExpandTokenStack_Click"
                        Content="="
                        Style="{StaticResource ExpandTokenStackButton}" />
                </Grid>
                <View:SkillDisplayer
                    x:Name="SkillDisplayer"
                    Grid.Column="1"
                    Width="0" />
                <View:TokenStack
                    x:Name="TokenStack"
                    Grid.Column="2"
                    Width="0" />
            </Grid>
            <View:GameSetterControlPanel
                x:Name="GameSetterPanel"
                Height="0"
                VerticalAlignment="Top"
                GameTheme="{Binding GameTheme}"
                Start="StartGame_Click" />
            <View:FileDragControl
                x:Name="FileDropArea"
                FileDraged="GameSave_FileDragged"
                IsHitTestVisible="False" />
            <View:TipBar
                x:Name="TipBar"
                Height="0"
                VerticalAlignment="Top" />
        </Grid>
    </Grid>
</Window>
